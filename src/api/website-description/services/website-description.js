'use strict';

/**
 * website-description service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::website-description.website-description');
